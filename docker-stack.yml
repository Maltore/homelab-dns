version: "3"

services:

  unbound:
    image: r.cfcr.io/maltore/maltore/homelabdns:master
    ports:
      - "53:53/udp"
      - "53:53"
    deploy:
      mode: replicated
      replicas: 5
      placement:
        constraints: [node.role == worker]
